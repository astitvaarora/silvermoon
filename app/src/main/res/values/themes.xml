<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SilentMoon" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="statusBarBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="greetingscreenstyle" parent="Base.Theme.SilentMoon">
        <item name="statusBarBackground">@color/blue</item>
    </style>

    <style name="Theme.SilentMoon" parent="Base.Theme.SilentMoon" />
</resources>